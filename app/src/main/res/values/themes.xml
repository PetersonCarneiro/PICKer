<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.PICKer" parent="Theme.Material3.Light">
        <!-- Customize your light theme here. -->
        <item name="android:windowBackground">@color/backgraund</item>
        <item name="android:action">@color/backgraund</item>
        <item name="colorPrimary">@color/black</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Theme.PICKer" parent="Base.Theme.PICKer" />
</resources>